# Application Configuration
APP_NAME=indexes-app
NODE_ENV=development

# Database Configuration
POSTGRES_DB=app_main
POSTGRES_USER=app_user
POSTGRES_PASSWORD=app_password
POSTGRES_PORT=5432
DATABASE_URL=postgresql://app_user:app_password@postgres:5432/app_main

# Ports Configuration
API_PORT=3000
WEB_PORT=5173
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
SFTP_PORT=2222

# Monitoring & Logging
OPENSEARCH_PORT=9200
OPENSEARCH_TRANSPORT_PORT=9300
OPENSEARCH_URL=http://opensearch:9200
OPENSEARCH_DASHBOARDS_PORT=5601
LOG_LEVEL=info
LOG_REQUESTS=false

# Authentication
COOKIE_SECRET=your-cookie-secret-here-change-in-production
SESSION_EXPIRATION_MINUTES=120

# Google OAuth (Optional)
GOOGLE_AUTH_CLIENT_ID=
GOOGLE_AUTH_CLIENT_SECRET=
GOOGLE_AUTH_CALLBACK_URL=http://localhost:3000/api/access/google/redirect
GOOGLE_AUTH_RETURN_URL=http://localhost:5173

# Frontend Configuration
FE_URL=http://localhost:5173
VITE_API_URL=http://localhost:3000

# CORS Configuration (Optional)
# Comma-separated list of additional allowed origins
# Example: CORS_ORIGIN=http://localhost:3001,https://app.example.com
CORS_ORIGIN=

# SFTP Configuration
SFTP_USERNAME=app_user
SFTP_PASSWORD=app_sftp_password

# Email Configuration (Optional)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# System User Configuration (for database seeding)
SYSTEM_USER_EMAIL=system@template.com
SYSTEM_USER_ALIAS=System
SYSTEM_USER_FIRST_NAME=System
SYSTEM_USER_LAST_NAME=User

# Digital Ocean Deployment (Optional)
DO_DEV_HOST=
DO_DEV_USER=root
DO_STAGING_HOST=
DO_STAGING_USER=root
DO_PROD_HOST=
DO_PROD_USER=root

# Frontend Configuration
VITE_SITE_NAME=My App
VITE_PLAUSIBLE_DOMAIN=localhost
VITE_PLAUSIBLE_URL=http://localhost:8000

# Plausible Analytics Configuration (Self-Hosted)
PLAUSIBLE_PORT=8000
PLAUSIBLE_BASE_URL=http://localhost:8000
PLAUSIBLE_DISABLE_REGISTRATION=true
PLAUSIBLE_DB_PASSWORD=plausible_password
PLAUSIBLE_SECRET_KEY=insecure-64-character-secret-key-for-development-only-change-this

# Plausible Auto-Configuration (No Manual Registration Required)
PLAUSIBLE_ADMIN_NAME=Admin User
PLAUSIBLE_ADMIN_EMAIL=admin@localhost
PLAUSIBLE_ADMIN_PASSWORD=admin123
PLAUSIBLE_SITE_DOMAIN=localhost
